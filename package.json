{
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm install --no-frozen-lockfile && nuxt build"
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "@nuxt/devtools": "^1.0.3",
    "nuxt": "^3.8.2",
    "vue": "^3.3.8",
    "vue-router": "^4.2.5"
  },
  "dependencies": {
    "openai": "^5.10.2"
  }
}
